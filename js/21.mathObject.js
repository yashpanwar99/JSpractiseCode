console.log("We'll learn math object");

// let z = window.Math;
// console.log(z);

let x;
x=Math.PI;
x=Math.LN10;
x=Math.E; 
x=Math.round(5.6);
x=Math.ceil(5.6);
x=Math.floor(5.6);
x=Math.abs(5.6);        // gives the absolute value
x=Math.sqrt(16);        // gives the squareroot
x=Math.pow(2,3);
x=Math.min(2,4,6,8);
x=Math.max(2,4,6,8);
x=Math.random(); // Gives no between 0 & 1
x=100*Math.random();  // Gives no b/w 0 & 100
x=50+(100-50)*Math.random();  // Gives between 50 & 100
x=Math.ceil(50+(100-50)*Math.random());
console.log(x);
